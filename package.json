{
  "name": "docker-save-registry-proxy",
  "version": "1.0.0",
  "description": "Docker Registry v2 compatible proxy backed by docker save tar streams.",
  "main": "src/index.js",
  "type": "commonjs",
  "scripts": {
    "start": "node src/index.js",
    "dev": "NODE_ENV=development node src/index.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "dotenv": "^17.4.2",
    "tar-stream": "^3.1.7"
  }
}
