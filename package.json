{
    "name": "docker-registry-proxy",
    "version": "2.0.0",
    "description": "Docker Registry API v2 compatible pull-through cache proxy — runs on Cloudflare Workers",
    "type": "module",
    "main": "src/index.js",
    "scripts": {
        "start": "wrangler dev --ip 0.0.0.0 --port 5000",
        "dev": "wrangler dev",
        "deploy": "wrangler deploy",
        "test": "node --test tests/*.test.js"
    },
    "engines": {
        "node": ">=18"
    },
    "license": "MIT",
    "devDependencies": {
        "wrangler": "^4.0.0"
    }
}
