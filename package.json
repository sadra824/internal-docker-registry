{
    "name": "docker-registry-proxy",
    "version": "2.0.0",
    "description": "Docker Registry API v2 compatible pull-through cache proxy — runs on Cloudflare Workers",
    "type": "module",
    "main": "src/index.js",
    "scripts": {
        "dev": "wrangler dev",
        "deploy": "wrangler deploy",
        "test": "node --test tests/*.test.js"
    },
    "engines": {
        "node": ">=18"
    },
    "license": "MIT",
    "devDependencies": {
        "wrangler": "^4.0.0"
    }
}
