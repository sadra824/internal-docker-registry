{
    "name": "docker-registry-proxy",
    "version": "1.0.0",
    "description": "Docker Registry API v2 compatible proxy that pulls images via dockerimagesave.akiel.dev",
    "main": "src/server.js",
    "scripts": {
        "start": "node src/server.js",
        "dev": "node --watch src/server.js"
    },
    "engines": {
        "node": ">=18"
    },
    "dependencies": {
        "dotenv": "^16.4.5",
        "express": "^4.19.2",
        "tar": "^7.4.3"
    }
}