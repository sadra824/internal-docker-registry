# نمونه‌ی متغیرهای محیطی برای اجرای محلی (npx wrangler dev)
# این فایل را به .dev.vars کپی کنید و در صورت نیاز تنظیم کنید (gitignored است)
#
# ⚠️ احراز هویت حذف شده است — pull کاملاً ناشناس است و credential لازم نیست.
# این فایل فقط برای مقادیر آینده (مثل توکن رجیستری خصوصی) نگه داشته شده است.
